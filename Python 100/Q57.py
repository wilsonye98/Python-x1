# Write a program to read an ASCII string and to convert it to a
# unicode string encoded by utf-8.
def ascii_to_unicode(ascii):
    unicode = ascii.encode(encoding='utf-8')
    print(u'{}'.format(u))

ascii_to_unicode('Hello world')
